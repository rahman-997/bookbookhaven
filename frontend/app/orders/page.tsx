'use client';

import Link from 'next/link';
import { useEffect, useState } from 'react';
import type { Order } from '@/lib/types';
import EmptyState from '@/app/components/EmptyState';
import { CheckIcon, PackageIcon } from '@/app/components/Icons';

const steps: Order['status'][] = ['pending','confirmed','shipped','completed'];
const statusTone: Record<Order['status'],string>={pending:'text-amber-200',confirmed:'text-sky-300',shipped:'text-violet-300',completed:'text-emerald-300',cancelled:'text-rose-300'};
export default function OrdersPage(){const[orders,setOrders]=useState<Order[]>([]);const[message,setMessage]=useState('Loading your order journal…');useEffect(()=>{void(async()=>{const r=await fetch('/api/backend/orders',{cache:'no-store'});if(r.status===401){setMessage('Sign in to view your orders.');return}if(!r.ok){setMessage('Could not load orders.');return}const p=await r.json();setOrders(p.data??[]);setMessage('')})()},[]);return <main className="page-shell pb-28 pt-12 md:pb-20 md:pt-16"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="section-kicker">Order journal</p><h1 className="mt-2 text-4xl font-black tracking-tight md:text-5xl">From shelf to doorstep.</h1><p className="mt-3 text-sm text-slate-500">Every purchase, status change and delivery destination in one place.</p></div><Link href="/" className="button button--ghost">Find another book</Link></div>{message?<div className="notice mt-8">{message} {message.startsWith('Sign')?<Link href="/login" className="ml-2 font-bold text-amber-200 underline">Sign in</Link>:null}</div>:null}<div className="mt-9 space-y-5">{orders.map(order=>{const current=steps.indexOf(order.status);return <article key={order._id} className="surface overflow-hidden rounded-[1.6rem]"><header className="flex flex-wrap items-start justify-between gap-4 border-b border-white/10 p-5 md:p-6"><div><p className="text-[10px] font-black uppercase tracking-[.15em] text-slate-600">Order {order._id.slice(-8).toUpperCase()}</p><p className="mt-1 font-bold">{new Date(order.createdAt).toLocaleString()}</p></div><span className={`chip capitalize ${statusTone[order.status]}`}>{order.status==='completed'?<CheckIcon size={13}/>:<PackageIcon size={13}/>} {order.status}</span></header>{order.status!=='cancelled'?<div className="p-5 md:p-6"><div className="grid grid-cols-4 gap-2">{steps.map((s,i)=><div key={s}><div className="progress-track"><div className="progress-fill" style={{width:i<=current?'100%':'0%'}}/></div><p className={`mt-2 text-[10px] font-bold capitalize ${i<=current?'text-slate-300':'text-slate-700'}`}>{s}</p></div>)}</div></div>:<div className="px-5 py-4 text-sm text-rose-300">This order was cancelled and its stock was restored.</div>}<div className="grid gap-6 border-t border-white/10 p-5 md:grid-cols-[1fr_280px] md:p-6"><div className="space-y-3">{order.items.map(item=><div key={`${order._id}-${item.book}`} className="flex justify-between gap-4 text-sm"><span className="text-slate-300">{item.quantity} × {item.title}</span><strong>${(item.quantity*item.unitPrice).toFixed(2)}</strong></div>)}</div><div className="md:border-l md:border-white/10 md:pl-6"><p className="text-[10px] font-black uppercase tracking-[.14em] text-slate-600">Delivery</p><p className="mt-2 text-sm leading-6 text-slate-400">{order.shippingAddress}</p><p className="mt-3 text-xs capitalize text-slate-600">{order.paymentMethod.replaceAll('_',' ')}</p><p className="mt-5 text-3xl font-black">${order.subtotal.toFixed(2)}</p></div></div></article>})}</div>{!message&&orders.length===0?<div className="mt-10"><EmptyState title="No orders yet" description="When a title becomes yours, its journey will appear here." icon={<PackageIcon size={27}/>} /></div>:null}</main>}
